# TYFINO parses provider payloads explicitly through streaming readers and does not rely on
# application-model reflection. Library-specific consumer rules are supplied by dependencies.
# Add narrowly scoped rules here only when an optimized release build demonstrates a need.
